// Brute force approach = O(m*n*k) or m^4

class Solution {
    public int[][] matrixBlockSum(int[][] mat, int k) {
        int n = mat.length;
        int m = mat[0].length;

        int ans[][] = new int[n][m];

        for(int i=0; i<n; i++){
            for(int j=0; j<m; j++){
                int sum = 0;

                for(int r=i-k; r<=i+k; r++){
                    for(int c=j-k; c<=j+k; c++){
                        if(r>=0 && r<n && c>=0 && c<m){
                            sum += mat[r][c];
                        }
                    }                
                }
                ans[i][j] = sum;
            }
        }
            return ans;
    }
}


// Optimal approach = O(m^2)

class Solution {
    public int[][] matrixBlockSum(int[][] mat, int k) {
       int n = mat.length;
        int m = mat[0].length;

        // Prefix Sum Matrix
        int[][] prefix = new int[n + 1][m + 1];

        // Build Prefix Sum
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= m; j++) {
                prefix[i][j] = mat[i - 1][j - 1]
                             + prefix[i - 1][j]
                             + prefix[i][j - 1]
                             - prefix[i - 1][j - 1];
            }
        }

        int[][] ans = new int[n][m];

        // Calculate Block Sum
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {

                int r1 = Math.max(0, i - k);
                int c1 = Math.max(0, j - k);

                int r2 = Math.min(n - 1, i + k);
                int c2 = Math.min(m - 1, j + k);

                ans[i][j] = prefix[r2 + 1][c2 + 1]
                          - prefix[r1][c2 + 1]
                          - prefix[r2 + 1][c1]
                          + prefix[r1][c1];
            }
        }

        return ans;
    }
}